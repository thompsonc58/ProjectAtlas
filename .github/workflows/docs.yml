name: Build Docs on: push: branches: \[ main \] pull_request: jobs:
validate: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 -
name: Validate Markdown run: \| echo "Repository checked out." find .
-name "\*.md"
